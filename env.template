# Newsletter Email Configuration
# Copy these variables to your Netlify Dashboard → Site settings → Environment variables
# OR create a .env file for local development (make sure .env is in .gitignore)

# ============================================
# SMTP Configuration for sending emails
# ============================================

# For Gmail (recommended for testing):
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=thetriastrophiles@gmail.com
SMTP_PASS=glit enzp uesz xqgd
FROM_EMAIL=thetriastrophiles@gmail.com

# For other SMTP services (SendGrid, Mailgun, etc.):
# SMTP_HOST=your-smtp-host.com
# SMTP_PORT=587
# SMTP_USER=your-smtp-username
# SMTP_PASS=your-smtp-password
# FROM_EMAIL=noreply@yourdomain.com

# ============================================
# Optional: Admin Notifications
# ============================================
# Email address to receive notifications for each new subscription
# If not set, notifications will be sent to FROM_EMAIL
ADMIN_EMAIL=admin@yourdomain.com

# ============================================
# Google Sheets Integration (Recommended)
# ============================================
# Store all subscriptions in a Google Sheet for easy viewing and management
# 
# Setup Instructions:
# 1. Create a Google Sheet and share it (or make it publicly editable if using API key)
# 2. Get your Sheet ID from the URL: https://docs.google.com/spreadsheets/d/SHEET_ID_HERE/edit
# 3. Enable Google Sheets API in Google Cloud Console
# 4. Create an API key in Google Cloud Console
# 5. Add the API key and Sheet ID below
#
# Note: For better security, consider using a Service Account instead of API key
GOOGLE_SHEETS_API_KEY=028ffb48a1c392b66eb59a8fa9a16617b59791b0
GOOGLE_SHEET_ID=1-DBPaMi9RFCAIxQCPS7f7r6XMoPIZSFD1Yp04kEYUXc

# ============================================
# Setup Instructions
# ============================================

# For Gmail:
# 1. Enable 2-Step Verification: https://myaccount.google.com/security
# 2. Generate an App Password: https://myaccount.google.com/apppasswords
# 3. Use the generated app password (16 characters) for SMTP_PASS
#    (NOT your regular Gmail password)

# For Netlify Deployment:
# 1. Go to: Netlify Dashboard → Your Site → Site settings → Environment variables
# 2. Click "Add a variable" for each variable above
# 3. Enter the variable name (e.g., SMTP_HOST) and value (e.g., smtp.gmail.com)
# 4. Click "Save" and redeploy your site

# For Local Development:
# 1. Copy this file to .env (make sure .env is in .gitignore)
# 2. Fill in your actual values
# 3. The .env file will be automatically loaded by Netlify CLI

